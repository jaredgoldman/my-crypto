describe('CsvController', () => {
  test('should export with a series of trades', async () => {})
  test('should export fail with no trades', async () => {})
  test('should create folder if trade folder isnt present', async () => {})
})
